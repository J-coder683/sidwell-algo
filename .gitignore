# Secrets — NEVER commit
.env
.env.local
.env.*.local
*.pem
*.key
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
build/
dist/

# Virtual environments
.venv/
venv/
env/
ENV/

# Test / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.tox/

# Sidwell cache (may contain copyrighted Damodaran data — do not redistribute)
.sidwell/
~/.sidwell/
*.cache

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log

# Antigravity / agent artifacts that aren't part of the project
.antigravity/

# Knowledge base — reference materials, mostly copyrighted (books, articles,
# transcripts). Used locally to inform framework specs, but never committed.
# Treat as an Obsidian vault — lives on disk, stays out of git.
knowledge/

# Local helper scripts that aren't part of the production pipeline
convert_reference.py
sa_data.txt
sa_test.html

aapl_financials.json

# PDF files
*.pdf

# Agent/verification scratch artifacts (not part of the project)
*_verify.json
verify_*.py
get_goldens.py
test_scrape.py
bergepaint_statements.json
scratch_*.py
old_test_*.py

# Scraped pages saved locally for inspection
*.html
*_files/

# Generated workbook/report outputs
exports/*.xlsx
output/*.md

# Local universe index + exchange master files (regenerable data)
*_index.json
EQUITY_L.csv
# ...but DO commit the prebuilt universe snapshot the deployed app loads offline
!data/universe_index.json
